import React from 'react';
import { AvatarProps } from './Avatar.types';
declare const Avatar: ({ onPress, name, size, src, backgroundColor }: AvatarProps) => React.JSX.Element;
export default Avatar;
